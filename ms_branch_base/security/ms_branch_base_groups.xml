<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="group_ms_branch_user" model="res.groups">
            <field name="name">Branch: own branches only</field>
            <field name="comment">Sees only documents belonging to the
                branches listed on their user record, plus documents with no
                branch set. Nobody is in this group until you put them in it,
                so installing the module changes nothing on an existing
                database.</field>
        </record>

        <record id="group_ms_branch_manager" model="res.groups">
            <field name="name">Branch: all branches</field>
            <field name="implied_ids" eval="[(4, ref('group_ms_branch_user'))]"/>
            <field name="comment">Sees every branch. Implies the branch user
                group so the two rules are combined and the wider one
                wins.</field>
        </record>
    </data>
</odoo>
