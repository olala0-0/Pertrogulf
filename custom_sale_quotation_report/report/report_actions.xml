<odoo>
    <!--
        We keep Odoo's standard "Print Quotation" button/action working as-is.
        Instead of creating a second print option, we point the EXISTING
        action at our new master template. Users see no change in the UI -
        same button, same menu entry - only the rendered content differs
        per company business_unit.
    -->
    <record id="action_report_saleorder_business_unit" model="ir.actions.report">
        <field name="name">Quotation / Order BU</field>
        <field name="model">sale.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">custom_sale_quotation_report.report_saleorder_business_unit</field>
        <field name="report_file">custom_sale_quotation_report.report_saleorder_business_unit</field>
        <field name="print_report_name">'Quotation - %s' % (object.name)</field>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="binding_type">report</field>
    </record>

    <record id="paperformat_proforma_invoice_ajman" model="report.paperformat">
        <field name="name">Proforma Invoice - Petro Gulf Ajman</field>
        <field name="default" eval="False"/>
        <field name="format">A4</field>
        <field name="orientation">Portrait</field>
        <field name="margin_top">0</field>
        <field name="margin_bottom">15</field>
        <field name="margin_left">0</field>
        <field name="margin_right">0</field>
        <field name="header_spacing">0</field>
        <field name="dpi">90</field>
    </record>

    <record id="action_report_proforma_invoice_ajman" model="ir.actions.report">
        <field name="name">Proforma Invoice</field>
        <field name="model">sale.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">custom_sale_quotation_report.report_proforma_invoice_ajman</field>
        <field name="report_file">custom_sale_quotation_report.report_proforma_invoice_ajman</field>
        <field name="print_report_name">'Proforma Invoice - %s' % (object.name)</field>
        <field name="paperformat_id" ref="paperformat_proforma_invoice_ajman"/>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="binding_type">report</field>
    </record>

    <record id="paperformat_tax_invoice_ajman" model="report.paperformat">
        <field name="name">Tax Invoice - Petro Gulf Ajman</field>
        <field name="default" eval="False"/>
        <field name="format">A4</field>
        <field name="orientation">Portrait</field>
        <field name="margin_top">0</field>
        <field name="margin_bottom">15</field>
        <field name="margin_left">0</field>
        <field name="margin_right">0</field>
        <field name="header_spacing">0</field>
        <field name="dpi">90</field>
    </record>

    <record id="action_report_tax_invoice_ajman" model="ir.actions.report">
        <field name="name">Tax Invoice</field>
        <field name="model">account.move</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">custom_sale_quotation_report.report_tax_invoice_ajman</field>
        <field name="report_file">custom_sale_quotation_report.report_tax_invoice_ajman</field>
        <field name="print_report_name">'Tax Invoice - %s' % (object.name)</field>
        <field name="paperformat_id" ref="paperformat_tax_invoice_ajman"/>
        <field name="binding_model_id" ref="account.model_account_move"/>
        <field name="binding_type">report</field>
    </record>

    <!--
        OPTIONAL (commented out): if you want THIS report to literally replace
        Odoo's default "Print" button instead of appearing as a second option
        in the print menu, uncomment below to hide the native action from the
        binding list. Left disabled by default since it's safer to test the
        new report alongside the original first.

    <record id="sale.action_report_saleorder" model="ir.actions.report">
        <field name="binding_type">no_bind</field>
    </record>
    -->
</odoo>
